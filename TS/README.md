# WebGL Triangle Demo

Small TypeScript + React + WebGL demo that renders a triangle. Shaders live in the `shaders/` folder.

Quick start:

1. cd TS
2. npm install
3. npm start

Project follows modular structure: `ShaderProgram`, `TriangleMesh`, `WebGLRenderer` and a React `App` that mounts canvas.
