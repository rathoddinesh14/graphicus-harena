%============================
% CHAPTER 4: SLICE EXTRACTION
%============================
\section{Slice Extraction: Cutting Through the Volume}

\begin{frame}
\frametitle{Chapter 4: The Art of Slicing}

\begin{columns}[T]
\begin{column}{0.6\textwidth}
  \textbf{Dr. Chen's Question:} "I have a 3D volume. How do I show the radiologist different views?"
  
  \vspace{0.5cm}
  \textbf{Orthogonal Slices} in medical imaging provide three fundamental views:
  \begin{itemize}
    \item \textbf{Axial}: Horizontal slices (like CT table position)
    \item \textbf{Sagittal}: Vertical slices from left to right
    \item \textbf{Coronal}: Vertical slices from front to back
  \end{itemize}
\end{column}
\begin{column}{0.35\textwidth}
  \begin{center}
    \begin{tikzpicture}[scale=0.8]
      % Patient orientation
      \draw[fill=medicalblue!10] (0,0) rectangle (2,3); % body
      \draw[fill=medicalgreen!20] (0.3,0.3) rectangle (1.7,2.7); % organs
      
      % Slice planes
      \draw[medicalred, thick, dashed] (1,0) -- (1,3); % sagittal
      \draw[medicalgreen, thick, dashed] (0,1.5) -- (2,1.5); % axial
      \draw[medicalblue, thick, dashed] (0.5,0) -- (0.5,3); % coronal
      
      \node[medicalred, font=\tiny] at (1.3,2.5) {Sagittal};
      \node[medicalgreen, font=\tiny] at (1.7,1.8) {Axial};
      \node[medicalblue, font=\tiny] at (0.2,2.5) {Coronal};
    \end{tikzpicture}
    \vspace{0.3cm}
    {\small\color{medicalgray}Anatomical Planes}
  \end{center}
\end{column}
\end{columns}

\clinicalexample{Clinical Views}
Radiologists use axial slices to count vertebrae, sagittal slices to assess spinal alignment, and coronal slices to evaluate lung fields.
\end{frame}

\begin{frame}[fragile]
\frametitle{Extracting Axial Slices: The Fast Path}
\begin{lstlisting}[language=JavaScript]
  const out = new Uint8Array(w * h);
  const z = Math.max(0, Math.min(depth - 1, index));
  const offset = z * w * h;
  const range = this.maxValue - this.minValue;

  for (let i = 0; i < w * h; i++) {
    // Linear normalization: [min, max] -> [0, 255]
    const norm = (this.data[offset + i] - this.minValue) / range;
    out[i] = Math.round(norm * 255);
  }
  return { data: out, width: w, height: h };
\end{lstlisting}
\begin{block}{Performance Advantage}
Axial slices are the fastest to extract because they correspond to a contiguous block of memory in our linear array.
\end{block}

\clinicalexample{Axial View in Practice}
In chest CT, axial slices show the lungs as two circular air-filled structures, with the heart and mediastinum in the center.
\end{frame}

\begin{frame}[fragile]
\frametitle{Improving Contrast: Windowing}
To see bone or soft tissue specifically, we apply a linear transform based on a \textbf{Center} ($C$) and \textbf{Width} ($W$):

\begin{lstlisting}[language=JavaScript]
const low = center - width / 2;
const high = center + width / 2;

// Inside the loop:
let val = this.data[offset + i];
let normalized = (val - low) / (high - low);
out[i] = Math.max(0, Math.min(255, normalized * 255));
\end{lstlisting}

\begin{table}
\small
\centering
\rowcolors{2}{softblue!20}{white}
\begin{tabular}{lrr}
\toprule
\textbf{Tissue} & \textbf{Center (HU)} & \textbf{Width (HU)} \\
\midrule
Bone & +400 & 1500 \\
Soft Tissue & +50 & 400 \\
Lung & -600 & 1500 \\
Brain & +35 & 80 \\
\midrule
\end{tabular}
\end{table}

\knowledgecheck{Windowing}
If a voxel has HU value of +200, what grayscale value does it get with soft tissue windowing (Center=+50, Width=400)?
\end{frame}

\begin{frame}[fragile]
\frametitle{Extracting Sagittal Slices: The Challenging Path}
\begin{lstlisting}[language=JavaScript]
  const out = new Uint8Array(D * H); // z x y
  const x = Math.max(0, Math.min(W - 1, index));
  for (let y = 0; y < H; y++) {
    const yOffset = y * W; // Row offset
    for (let z = 0; z < D; z++) {
      // Manual Indexing: x + yW + zWH
      const volumeIdx = x + yOffset + (z * W * H);
      const voxel = this.data[volumeIdx];
      const outIdx = y * D + z;
      out[outIdx] = ((voxel - min) / range) * 255;
    }
  }
  return { data: out, width: D, height: H };
\end{lstlisting}

\begin{alertblock}{Performance Challenge: Non-Contiguous Access}
Unlike Axial slices, Sagittal voxels are separated by exactly \texttt{width} elements (for $y$) and \texttt{width * height} elements (for $z$). This results in poor \textbf{cache locality}.
\end{alertblock}

\clinicalexample{Sagittal View}
Sagittal slices through the spine show vertebral bodies stacked like a pile of coins, with the spinal canal running through the center.
\end{frame}

\begin{frame}[fragile]
\frametitle{Extracting Coronal Slices: The Balanced Approach}
\begin{lstlisting}[language=JavaScript]
  const out = new Uint8Array(D * W); // z x x
  const y = Math.max(0, Math.min(H - 1, index));
  const yOffset = y * W; // Constant row offset
  for (let x = 0; x < W; x++) {
    for (let z = 0; z < D; z++) {
      // Indexing: x + (y-const) + z*(W*H)
      const volumeIdx = x + yOffset + (z * W * H);
      const outIdx = x * D + z;
      const voxel = this.data[volumeIdx];
      out[outIdx] = ((voxel - min) / range) * 255;
    }
  }
  return { data: out, width: D, height: W };
\end{lstlisting}

\begin{block}{Memory Access Pattern}
The Coronal plane samples voxels that are exactly $W \cdot H$ units apart in the linear array for each $z$ step.
\end{block}

\knowledgecheck{Slice Orientation}
A sagittal slice at x=256 in a 512×512×200 volume will be 200×512 pixels. Why? Draw the indexing pattern.
\end{frame}

\begin{frame}
\frametitle{Slice Extraction Complexity: Performance Matters}

\textbf{Time Complexity:}
\begin{itemize}
  \item Axial: $O(W \times H)$ - single plane lookup
  \item Sagittal: $O(W \times D)$ - multiple lookups per row
  \item Coronal: $O(W \times D)$ - multiple lookups per column
\end{itemize}

\vspace{0.5cm}

\textbf{Normalization:}
\begin{itemize}
  \item Convert float voxels to 8-bit grayscale
  \item Compute min/max during volume construction
  \item Apply linear mapping:
  $$\text{output} = \frac{\text{voxel} - \text{min}}{\text{max} - \text{min}} \times 255$$
  \item Enables proper visualization of contrast
\end{itemize}

\begin{alertblock}{Real-time Requirement}
For interactive viewing, slice extraction must complete in <16ms (60 FPS). Large volumes require optimization techniques.
\end{alertblock}

\knowledgecheck{Performance Optimization}
Why is axial slice extraction faster than sagittal? How could you optimize the slower slice types?
\end{frame}